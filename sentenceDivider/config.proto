encoding "utf8";

TTextMinerConfig {

    // Корневой словарь
    Dictionary = "/home/vagrant/CL_Lab/Sem/sentenceDivider/segmentator_dict.gzt";

    // PrettyOutput = "pretty.html"; // путь к файлу с отладочным выводом в удобном для чтения виде

    // Формат вывода данных
    Output = {
        Format = text;
    }
}


//    Input = {
//      File = "text.txt"; // путь к входному файлу
//    }
//    }

    // Количество потоков
    // NumThreads = 4;