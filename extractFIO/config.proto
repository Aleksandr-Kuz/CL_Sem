encoding "utf8";

TTextMinerConfig {

    // Корневой словарь
    Dictionary = "/home/vagrant/CL_Lab/Sem/extractFIO/dict.gzt";

    //PrettyOutput = "pretty.html"; // путь к файлу с отладочным выводом в удобном для чтения виде

//    Input = {
//      File = "sentences.txt"; // путь к входному файлу
//    }

    Articles = [
      { Name = "FIOExtract" }
    ]

    Facts = [
      { Name = "PersonFact" }
    ]

    // Формат вывода данных
    Output = {
        Format = xml;
    }
}